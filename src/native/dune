(library
 (name jsonkit)
 (public_name jsonkit)
 (wrapped false)
 (libraries
  (re_export yojson)))

; Platform-neutral sources shared with the Melange library in ../ ; only the
; classify layer and [Jsonkit_json] itself are backend-specific.

(copy_files
 (files ../jsonkit.ml))

(copy_files
 (files ../jsonkit_json_errors.ml))

(copy_files
 (files ../jsonkit_jsonschema.ml))

(copy_files
 (files ../jsonkit_jsonschema_primitives.{ml,mli}))
